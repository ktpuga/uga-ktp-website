// Authentik configuration
AUTHENTIK_CLIENT_ID=
AUTHENTIK_CLIENT_SECRET=
AUTHENTIK_ISSUER=

// Auth.js v5
AUTH_SECRET=
AUTH_URL=

// Shared backend API (server-side only — do not prefix with NEXT_PUBLIC_)
API_URL=http://localhost:3000