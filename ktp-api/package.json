{
  "name": "ktp-api",
  "version": "0.1.0",
  "private": true,
  "description": "Test API scaffold for the KTP app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "db:init": "node scripts/init-db.js",
    "db:seed": "node scripts/init-db.js --seed-only",
    "db:schema": "node scripts/init-db.js --schema-only",
    "db:seed-photos": "node scripts/seed-photos.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.3",
    "jose": "^6.2.3",
    "multer": "^2.2.0",
    "pg": "^8.13.1"
  }
}
