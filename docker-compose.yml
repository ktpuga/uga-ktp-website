services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://ugaktp.com}
        NEXT_PUBLIC_SANITY_PROJECT_ID: ${NEXT_PUBLIC_SANITY_PROJECT_ID:-7qe12arj}
        NEXT_PUBLIC_SANITY_DATASET: ${NEXT_PUBLIC_SANITY_DATASET:-production}
        NEXT_PUBLIC_SANITY_API_VERSION: ${NEXT_PUBLIC_SANITY_API_VERSION:-2025-06-04}
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      API_URL: http://api:4000
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_URL: ${AUTH_URL}
      AUTHENTIK_CLIENT_ID: ${AUTHENTIK_CLIENT_ID}
      AUTHENTIK_CLIENT_SECRET: ${AUTHENTIK_CLIENT_SECRET}
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - ktp

  api:
    build:
      context: ./ktp-api
      dockerfile: Dockerfile
    ports:
      # Exposed externally so the iOS app can reach it
      - "${API_PORT:-4000}:4000"
    environment:
      PORT: 4000
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      AUTHENTIK_ISSUER: ${AUTHENTIK_ISSUER}
    restart: unless-stopped
    networks:
      - ktp

networks:
  ktp:
    driver: bridge
